# 2026-04-24 작업 기록

## 오늘 기준 최고 기록

- 현재 최고 public: `10.1064209775`
- 파일: `submission_a101_10.csv`

## 오늘 진행한 실험

### 1. a107 public 확인

- 제출 파일: `submission_a107_10.csv`
- public 결과: `10.1067154934`

해석:
- `a101_10`을 넘지는 못했지만 차이가 매우 작음
- correction layer를 더 얹는 것보다 `shift-heavy expert` 자체를 다시 학습하는 방향이 더 건강하다는 신호

### 2. a108 설계 및 검증

핵심 아이디어:
- `a101`의 큰 구조는 유지
- `shift-heavy expert`만 pseudo-group 기반으로 다시 학습
- correction layer는 추가하지 않음

중간에 중요한 이슈를 확인했습니다.

- scenario hardness를 feature에 직접 넣었더니 로컬에서 비정상적으로 좋은 점수가 나옴
- 실제 개선이 아니라 leakage-like 착시로 판단
- hardness는 feature에서 제거하고 weighting에만 사용하도록 수정

수정 후 결과:
- `a108` direct router OOF: `7.4219`
- public 제출: `submission_a108_12.csv`
- public 결과: `10.1122539111`

해석:
- pseudo-group 방향 자체는 가능성이 없지 않음
- 하지만 현재 구현은 아직 제출용 카드가 아님
- hardness는 direct feature가 아니라 weighting/selection 보조 signal로만 써야 안전함

## 오늘 얻은 결론

1. `a100 family`는 여전히 맞는 방향이다.
2. `a101_10`은 아직 가장 강한 public 앵커다.
3. `a101` 이후 correction layer를 더 얹는 방향은 계속 public에서 실패했다.
4. `a107`은 expert 재학습 방향이 더 건강하다는 점을 보여줬다.
5. `a108`은 pseudo-group를 쓰더라도 너무 복잡하거나 hardness를 직접 feature로 넣으면 위험하다는 점을 보여줬다.

## 내일 방향

다음 단계는 `a109` 쪽으로 아래와 같이 생각하고 있다.

- `a101_10` 앵커 유지
- correction layer 추가는 잠시 중단
- `shift-heavy expert` 재학습 방향 유지
- pseudo-group는 더 단순하고 물리적인 extreme 정의를 우선 사용
- validation을 average OOF보다 `worst-group / adversarial subset / unseen-like` 기준으로 재설계

## 한 줄 요약

오늘은 `a107`로 expert 재학습 방향이 더 건강하다는 점을 확인했고, `a108`로 pseudo-group 접근의 leakage-like 함정까지 잡아냈다. 내일부터는 correction layer가 아니라 validation과 expert 학습 기준을 더 정교하게 다듬는 쪽이 맞다.
