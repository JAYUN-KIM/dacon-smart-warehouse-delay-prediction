# 2026-04-25 작업 기록

## 오늘 기준 최고 기록

- 현재 최고 public: `10.1064209775`
- 파일: `submission_a101_10.csv`

## 오늘 진행한 실험

### 1. a109 public 확인

- 제출 파일: `submission_a109_09.csv`
- public 결과: `10.1085982977`

해석:
- `a108`보다 훨씬 정상적이고 validation 설계 방향도 더 맞았음
- 하지만 `a101_10`을 넘기기에는 부족했음
- 단순히 `worst-group / testlike / baseline_hi` 기준으로 후보를 다시 고르는 것만으로는 큰 점프가 어렵다는 점을 확인

### 2. a110 설계 및 검증

핵심 아이디어:
- 이 문제를 “현재 상태만 맞추는 회귀”가 아니라
- “같은 시나리오 안의 25슬롯 전체 맥락을 활용해 가까운 미래의 지연 압력을 읽는 문제”로 다시 해석

새로 넣은 핵심 피처:
- `lead1`, `fut2`, `fut3`
- `future_load_per_robot`
- `future_pressure_gap`
- `future_work_gap`
- `future_battery_gap`
- `future_instability`
- `future_stress_score`

로컬 결과:
- `a110` direct router OOF: `7.4081`
- best candidate OOF: `7.7730`

public 제출:
- 제출 파일: `submission_a110_09.csv`
- public 결과: `10.106859334`

해석:
- 최고 기록은 못 넘었지만 `a102~a106` correction layer 계열과는 다르게 건강한 방향의 실패였음
- future-window 해석 자체는 실제로 신호가 있다는 점을 보여줌
- 다만 hard subset이나 공격적 selection으로 밀기보다는 backbone 수준에서 더 안정적으로 흡수해야 함

## 오늘 얻은 결론

1. `a109`는 validation과 candidate 선택 기준을 public 쪽으로 다시 맞추는 데 의미가 있었지만, selection만으로는 부족했다.
2. `a110`은 future-window 기반 창고 부하 해석이 실제로 유효한 backbone 신호라는 점을 보여줬다.
3. `a101_10`은 여전히 가장 강한 public 앵커다.
4. correction layer를 다시 얹는 방향보다, `a101`의 강한 자산 위에 미래 부하 해석을 더 정교하게 결합하는 쪽이 맞다.

## 다음 방향

다음 단계는 아래처럼 정리한다.

- `a101_10` 앵커 유지
- correction layer 추가는 계속 보류
- `shift-heavy expert` 재학습 방향 유지
- `future-window` 기반의 창고 부하 피처를 backbone 쪽으로 더 정교하게 확장
- average OOF보다 `worst-group / adversarial subset / unseen-like / future-stress` 중심 validation 강화

## 한 줄 요약

오늘은 `a109`로 validation과 선택 기준만 바꾸는 데 한계가 있다는 점을 확인했고, `a110`으로 미래 부하 요약이 실제 신호라는 점을 확인했다. 다음은 correction layer가 아니라, `a101` 앵커 위에 future-window 해석을 더 안정적으로 흡수하는 방향이 맞다.
