# 2026-04-20

## 오늘 한 일

- `a92`
  - EDA 결론을 바탕으로 `scenario baseline + slot deviation + regime router` 구조를 구현
  - 단독 direct 모델보다 `shift subset` 보정 신호로 쓰는 쪽이 더 의미 있다는 점을 다시 확인

- `a93`
  - scenario baseline specialist만 따로 떼어 검증
  - shift 시나리오에 baseline correction을 얹는 구조였지만, `a88`을 넘는 카드로 보이진 않았음

- `a94`
  - `a88`의 representation residual
  - `a92`의 scenario baseline signal
  - `shift + high cluster + unseen` combo specialist
  를 통합
  - `submission_a94_51.csv`로 최고 기록 `10.1133848903` 달성

- `a95`
  - `a94`를 더 세분화해서 `combo_q90`, `combo_q95` specialist를 따로 학습
  - 구조적 의미는 있었지만 `a94`를 바로 대체할 수준은 아니었음

- `a96`
  - `a94_51` 앵커를 보수적으로 미세 보정
  - 안정적이지만 강한 새 카드라고 보긴 어려움

## 오늘의 핵심 결론

- 최근 가장 의미 있는 점프는 `a94`에서 나왔음
- `representation + baseline + combo regime`를 함께 보는 방식이 실제로 먹혔음
- `a95`, `a96`을 통해 `a94`를 더 세게 나누는 것만으로는 아직 부족하다는 점도 확인함

## 현재 최고 기록

- 점수: `10.1133848903`
- 파일: `submission_a94_51.csv`

## 다음 방향

- `a97`
  - `a94 family`를 유지하되 더 안정적인 specialist로 갈지
  - 아니면 완전히 다른 신호를 추가할지
  를 판단하면서 진행

