# 2026-04-26 작업 기록

## 오늘 기준 최고 기록

- 현재 최고 public: `10.103316418`
- 파일: `submission_a114_09.csv`

## 오늘 진행한 실험

### 1. a111_v2 외부 파이프라인 점검

외부에서 받은 `a111_ood_router_pipeline_v2.py`를 실제로 실행해봤습니다.

결론:
- 구조적으로는 정돈되어 있었음
- `time_idx` 자동 생성, future feature 안정화, fold-local weighting 등 참고할 점은 있었음
- 하지만 raw 중심 standalone routed-z backbone이라 성능이 너무 약했음
- `a101/a110`을 대체할 backbone은 아니었고, 운영/구조 참고용에 가까웠음

### 2. a112: 미래창 피처 과확장 실험

질문:
- 미래창 피처를 300개 이상으로 늘리면 실제로 점프가 나오는가?

실험:
- 총 피처 수 `348`
- 미래창 관련 피처 `220`

결과:
- `a112 direct_router_mae = 7.4448`
- best candidate OOF `7.7841`

해석:
- 미래창 해석이 틀린 건 아님
- 하지만 row feature 쪽으로 너무 많이 늘리면 오히려 신호가 희석됨

### 3. a113: compact future row backbone

`a112`보다 줄인 버전도 돌려봤습니다.

결과:
- `a113 direct_router_mae = 7.4633`
- best candidate OOF `7.7857`

해석:
- 단순히 개수만 줄인다고 해결되지 않음
- row expert 쪽으로 future 신호를 과하게 확장하는 방향 자체가 약함

### 4. a114: future scenario baseline 실험

이번엔 미래창 정보를 row expert 확장 대신, **scenario baseline/scale 강화용**으로 넣었습니다.

결과:
- `a114 direct_router_mae = 7.3957`
- best candidate OOF `7.7759`
- public: `10.103316418`

해석:
- 현재까지 미래창 계열 중 가장 성공적
- 미래창 신호는 “더 많이”보다 “어디에 넣느냐”가 중요함
- scenario-level stress 해석으로 넣었을 때 실제 public 개선이 나옴

### 5. a115: a114 후보 생성 정밀화

`a114` backbone은 그대로 두고, `alpha_direct / fallback / confidence threshold`만 촘촘하게 다시 튜닝했습니다.

로컬:
- best candidate OOF `7.7112`

public:
- `submission_a115_146.csv`
- 결과 `10.1066275842`

해석:
- 로컬 기준으론 매우 좋아졌지만 public에서는 `a114`를 넘지 못함
- 이제 이 family 안에서 candidate generation만 더 깎는 방식은 한계에 가까움

## 오늘 얻은 핵심 교훈

1. 미래창 해석은 맞다.
2. 하지만 미래창 피처를 무작정 늘리는 것은 독이다.
3. 가장 잘 먹힌 건 미래 부하/압력 요약을 `scenario baseline/scale` 강화용으로 넣는 방식이다.
4. `a115` 결과를 보면 이제 같은 family 안에서 calibration만 더 미세하게 다듬는 건 9점대 진입용 카드가 아니다.

## 다음 방향

현재 판단은 아래와 같습니다.

1. `a114_09`를 메인 public 앵커로 유지
2. correction layer 추가는 계속 중단
3. future-window 해석은 유지
4. 다만 더 많은 피처 확대보다 scenario-level stress 해석에 집중
5. 다음은 `a114`가 왜 먹혔는지를 살린 **새 direct family(a116)** 설계가 더 빠름

## 한 줄 요약

오늘은 미래창 피처를 많이 늘리는 건 답이 아니라는 점을 확인했고, `a114`처럼 scenario baseline/scale에 정확하게 넣었을 때만 실제 public 개선이 나온다는 걸 확인했습니다. 동시에 `a115`로 같은 family 안 미세조정의 한계도 확인했기 때문에, 다음은 새 backbone 설계 쪽으로 넘어가는 게 맞습니다.
