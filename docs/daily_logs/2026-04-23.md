# 2026-04-23 작업 기록

## 오늘의 기준점

- 현재 최고 public: `10.1064209775`
- 파일: `submission_a101_10.csv`

## 오늘 진행한 실험

### 1. a106 public 확인

- `submission_a106_20.csv`
- public 결과: `10.11045386`

해석:
- `a105`보다 더 안전한 continuous correction을 만들려는 시도였지만, public에서는 여전히 `a101_10`을 넘지 못했다.
- 이로써 `a101` 이후 correction layer를 얹는 실험들은 모두 public에서 최고 기록을 넘지 못했다.

### 2. a107 설계 및 실행

- correction layer를 더 얹는 대신, `shift-heavy expert` 자체를 다시 학습
- 물리적 extreme 그룹 기준 사용
  - `order_inflow_15m` 상위
  - `pack_pressure` 상위
  - `work_pressure` 상위
  - `robot_active` 하위

핵심 로컬 결과:
- `a101` direct router OOF: `7.4224`
- `a107` direct router OOF: `7.4162`

해석:
- 전체 라우터 기준으로는 미세 개선
- 하지만 extreme group에서 `shift_jtt` expert가 `global`을 압도할 정도는 아니었음
- 즉 방향은 건강하지만, 아직 이득은 작음

## 오늘 얻은 결론

1. `a100 family`는 여전히 메인 방향이다.
2. `a101_10`은 여전히 가장 강한 public 앵커다.
3. `a101` 이후 correction layer 추가는 public에서 반복적으로 실패하고 있다.
4. 따라서 다음은 correction magnitude를 더 깎는 것보다, expert 재학습과 validation 재설계를 더 밀어야 한다.

## 다음 방향

- `a101_10` 앵커 유지
- correction layer 추가보다 `shift-heavy expert` 재학습
- pseudo-group 정의를 더 정교하게 보정
- average OOF보다 worst-group / adversarial subset 관점 강화

## 한 줄 요약

오늘은 “correction을 더 잘 얹는 것”보다 “어려운 구간을 담당할 expert를 더 잘 학습시키는 것”이 다음 단계라는 점을 더 분명하게 확인한 날이었다.
