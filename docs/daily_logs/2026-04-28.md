# 2026-04-28 작업 기록

## 오늘 기준 최고 기록

- 현재 최고 public: `10.1005923422`
- 파일: `submission_a117_09.csv`

## 오늘 제출한 결과

### 1. a118: asymmetric clipped delta

전날 만든 `submission_a118_01.csv`를 제출했습니다.

구성:
- `a117_09`의 안전한 평균/최댓값 대역을 기준으로 positive/negative delta를 비대칭 클리핑
- OOF MAE: `7.7275009155`
- test mean: `18.356560`
- test max: `44.054348`

public:
- `10.1018318718`

해석:
- OOF만 보면 좋아 보였지만 public에서는 악화됐습니다.
- 최댓값을 낮추고 비대칭 클리핑을 적용하면 더 안전할 것이라는 가설이 깨졌습니다.
- `a117_09`에서 분포 shape를 조금만 바꿔도 public이 민감하게 나빠질 수 있음을 확인했습니다.

### 2. a119: public-safe microgrid

`a118` 실패 이후, `a117_09`를 거의 그대로 유지하는 초근접 후보를 만들었습니다.

선택 후보:
- `submission_a119_03.csv`
- 방식: `a117_09`의 `q98 clip`을 `q98.5`로 아주 살짝 풀어준 후보
- OOF MAE: `7.760396`
- `a117_09` 대비 OOF 개선: 약 `0.00693`
- `a117_09` 대비 test 평균 변화량: 약 `0.00055`
- test mean: `18.359195`
- test max: `44.13235`

public:
- `10.1010915332`

해석:
- 거의 동일한 분포를 유지했지만 public 개선으로 이어지지 않았습니다.
- 이 결과는 clipped ridge delta 계열이 `a117_09` 근처에서 이미 포화됐다는 신호로 봅니다.

## 오늘의 결론

1. `a117_09`는 여전히 최고 기록입니다.
2. `a118`은 OOF와 분포 안정성만으로는 public을 예측하기 어렵다는 것을 보여줬습니다.
3. `a119`는 `a117_09`와 거의 같아도 개선이 나오지 않는다는 것을 보여줬습니다.
4. 따라서 ridge delta 계열의 alpha, clip quantile, tail tempering을 더 미세하게 조정하는 것은 기대값이 낮습니다.
5. 다음 개선은 같은 family 내부가 아니라, `a117_09`가 구조적으로 틀리는 scenario group을 새로 찾아야 합니다.

## 다음에 해야 할 일

1. `a117_09`의 OOF residual을 기준으로 오류 상위 시나리오를 다시 EDA
2. high-error scenario의 공통 특징을 `future_stress`, `robot_active`, `order_inflow`, `pressure`, `layout`, `time profile` 축으로 분해
3. ridge delta와 상관이 낮은 새 독립 신호를 찾기
4. 새 신호가 없으면 같은 clipped delta family를 더 밀지 않기

## 한 줄 요약

오늘은 마지막 제출로 `a119_03 = 10.1010915332`까지 확인했고, 최고 기록은 `a117_09 = 10.1005923422`로 유지됐습니다. 이제 ridge delta 미세조정보다 `a117_09`의 실패 그룹을 다시 찾는 쪽이 9점대 진입에 더 중요합니다.
