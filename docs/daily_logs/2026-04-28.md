# 2026-04-28 작업 기록

## 오늘 기준 최고 기록

- 최고 public: `10.0967991272`
- 파일: `submission_a122_1045.csv`
- 이전 최고: `submission_a117_09.csv = 10.1005923422`

## 1. a118: asymmetric clipped delta

전날 만든 `submission_a118_01.csv`를 제출했습니다.

구성:
- `a117_09`를 기준으로 positive/negative delta를 비대칭 clipping
- OOF MAE: `7.7275009155`
- test mean: `18.356560`
- test max: `44.054348`

public:
- `10.1018318718`

해석:
- OOF 기준으로는 좋아 보였지만 public에서는 악화됐습니다.
- `a117_09`의 분포 shape를 조금만 바꿔도 public이 민감하게 흔들릴 수 있음을 확인했습니다.

## 2. a119: public-safe microgrid

`a117_09`와 거의 같은 형태를 유지하는 초근접 후보를 생성했습니다.

대표 후보:
- `submission_a119_03.csv`
- 방식: `q98 clip`을 `q98.5`로 아주 약하게 이동
- OOF MAE: `7.760396`
- `a117_09` 대비 test 평균 변화량: 약 `0.00055`

public:
- `10.1010915332`

해석:
- 거의 동일한 분포를 유지해도 public 개선으로 이어지지 않았습니다.
- clipped ridge delta family는 `a117_09` 근처에서 포화에 가깝다고 판단했습니다.

## 3. a120: scenario bias calibrator

`a117_09` residual을 scenario-level feature로 보정하는 bias calibrator를 시도했습니다.

결과:
- OOF 개선폭이 약 `0.00027` 수준
- test 변화량도 매우 작음
- 제출하지 않는 쪽으로 판단

해석:
- 단순 scenario bias correction은 지금 필요한 레버가 아니었습니다.

## 4. a121: high-error group EDA

`a117_09`가 구조적으로 틀리는 구간을 다시 찾기 위해 high-error scenario를 분석했습니다.

주요 발견:
- scenario MAE top 10% threshold: `19.055895`
- high-error scenario mean MAE: `36.048248`
- rest scenario mean MAE: `4.625003`
- high-error scenario mean bias: `+26.686848`

강한 신호:
- late slot에서 오차와 양수 residual이 커짐
- `pack_pressure` high q90: MAE lift `13.784`, bias `+10.598`
- `pack_utilization` high q90: MAE lift `10.610`, bias `+8.178`
- `future_stress_score` high q90: MAE lift `6.266`, bias `+5.166`

해석:
- high-error group은 랜덤 노이즈가 아니라 구조적인 과소예측이었습니다.
- 특히 late/high-pressure/high-utilization/high-stress 구간에서 실제 지연을 낮게 보고 있었습니다.

## 5. a122: late/high-stress small uplift

`a121`에서 찾은 신호를 바탕으로 `a117_09` 앵커를 유지하고, stress risk가 높은 일부 구간에만 작은 양수 uplift를 적용했습니다.

대표 후보:
- `submission_a122_1045.csv`
- risk: `all_stress_small`
- bins: `10`
- stat: `median`
- shrink: `0.5`
- OOF MAE: `7.7663116455`
- `a117_09` 대비 test 평균 uplift: `0.054897`
- max uplift: `0.393809`

public:
- `10.0967991272`

해석:
- 기존 최고 `10.1005923422`를 갱신했습니다.
- late/high-stress 과소예측 보정이 public에서도 통한다는 것을 확인했습니다.
- 단순한 micro tuning이 아니라, 오차 원인을 다시 찾고 그 구간만 작게 보정한 것이 효과를 냈습니다.

## 오늘의 결론

1. `a117_09` 근처의 delta micro tuning은 포화됐습니다.
2. high-error group을 다시 분석한 것이 전환점이었습니다.
3. `a122_1045`가 10.10 벽을 깨며 새 최고 기록을 만들었습니다.
4. 다음 단계는 `a122_1045` family를 중심으로 risk score와 uplift 강도를 더 정밀하게 다듬는 것입니다.

## 다음 작업

- `a123`에서 `a122_1045` 주변 후보를 정밀 탐색
- `all_stress_small` risk score 구성요소 조정
- shrink를 `0.35~0.65` 근처에서 더 촘촘하게 탐색
- 평균 uplift와 max uplift가 과해지지 않도록 public-safe 후보 분리
- late/high-pressure/high-utilization 과소예측 구간을 더 정확히 좁히기
