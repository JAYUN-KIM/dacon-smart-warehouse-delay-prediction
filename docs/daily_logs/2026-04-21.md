# 2026-04-21 작업 기록

## 오늘의 최고 점수

- `submission_a101_10.csv`
- `10.1064209775`

## 오늘 한 일

### 1. a100 구조 실험

- `baseline + scale + routed deviation`
  구조로 문제를 다시 정의했습니다.
- 기존 `a94` specialist 계열을 그대로 더 다듬는 대신,
  target space 자체를 분해해서 expert가 deviation을 맡도록 바꿨습니다.

결과:
- `a100_05 = 10.1090848449`

이 점수로,
이제 다음 메인 family는 `a100 family`라는 판단이 더 분명해졌습니다.

### 2. a101 soft router 실험

`a100`을 그대로 쓰지 않고,

- robust baseline / scale
- soft expected-error router
- confidence fallback

을 추가한 `a101`을 실험했습니다.

결과:
- `a101_10 = 10.1064209775`

즉,
`soft routing`이 실제로 `best single expert`보다 더 나은 방향이라는 점을 확인했습니다.

## 오늘의 해석

오늘 얻은 가장 중요한 결론은 이겁니다.

1. `a94 family`는 여전히 강한 기준점이지만, 이제 메인 축은 아니다.
2. 더 큰 개선은 `a100 family`처럼 decomposition + routing 구조에서 나온다.
3. 다음은 모델 하나를 더 추가하는 것이 아니라, router를 더 잘 만드는 쪽이 맞다.

## 다음 방향

다음 실험에서는 아래를 더 볼 계획입니다.

1. support / shift feature를 router 입력에 더 추가하기
2. scale 예측을 더 안정화하기
3. fallback 규칙을 더 보수적으로 다듬기
4. expert별 expected error 예측을 더 정확하게 만들기

즉,
이제 핵심은 “어떤 모델이 제일 잘 맞느냐”보다
“어떤 샘플에서 어떤 expert를 얼마나 믿을지”입니다.
