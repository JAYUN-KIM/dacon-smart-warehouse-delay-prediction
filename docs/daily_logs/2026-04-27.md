# 2026-04-27 작업 기록

## 오늘 기준 최고 기록

- 현재 최고 public: `10.1005923422`
- 파일: `submission_a117_09.csv`
- 이전 최고: `submission_a114_09.csv = 10.103316418`

## 오늘 진행한 실험

### 1. a116: ridge seq2seq direct family

`a114_09` 이후 같은 future-window family의 candidate calibration만으로는 한계가 보였기 때문에, 25개 슬롯을 한 번에 예측하는 scenario-level ridge seq2seq 계열을 새로 시도했습니다.

주요 결과:

- feature count: `950`
- anchor OOF MAE: `7.7799119949`
- direct ridge OOF MAE: `7.9801287651`
- best ridge alpha: `500.0`

해석:

- direct ridge 모델 자체는 앵커보다 약했습니다.
- 하지만 앵커와 다른 방향의 예측 delta를 만들어냈고, 이 delta가 얇게 섞이면 public에서 도움이 될 가능성이 보였습니다.
- 따라서 `a116`은 단독 제출 모델이 아니라 delta provider로 재해석했습니다.

### 2. a117: clipped ridge delta blend

`a116` direct prediction을 그대로 섞지 않고, `a114` 앵커와의 차이만 clipping한 뒤 낮은 alpha로 반영했습니다.

대표 후보:

- `submission_a117_09.csv`
  - 설정: `alpha=0.08`, `q98 clip`
  - OOF MAE: `7.7673277855`
  - public: `10.1005923422`
- 더 강한 계열
  - public: `10.1014682069`
  - 해석: 방향은 맞지만 강도를 더 올리면 흔들림

핵심 결론:

- `a116` direct family는 단독으로 약하지만 delta 정보는 유효했습니다.
- public에서 먹힌 것은 “더 큰 correction”이 아니라 “잘린 delta를 얇게 반영하는 방식”이었습니다.
- `a117_09`가 기존 최고 `a114_09`를 갱신하면서, clipped ridge delta 방향이 실제 public-valid임을 확인했습니다.

### 3. a118: asymmetric clipped delta 후보 생성

`a117_09`의 성공 구간을 기준으로, delta를 positive/negative 방향으로 나눠 비대칭 클리핑하는 후보를 만들었습니다.

초기 OOF 1등 후보는 correction 강도가 다소 컸습니다.

- OOF MAE: `7.7113685608`
- test mean: `18.394634`
- test max: `44.444366`

하지만 이 후보는 `a117_18` 계열처럼 평균과 최댓값이 올라가 public에서 흔들릴 위험이 있어 제출 우선순위에서 제외했습니다.

최종 우선 후보:

- `submission_a118_01.csv`
- OOF MAE: `7.7275009155`
- test mean: `18.356560`
- test max: `44.054348`
- `a117_09` 대비 평균 변화량: 약 `0.0244`

판단:

- `a118_01`은 OOF 1등은 아니지만, public에서 성공한 `a117_09`의 분포 shape에 더 가깝습니다.
- 내일 첫 제출 후보로 적합합니다.

## 오늘 얻은 핵심 교훈

1. direct model 자체가 약해도, 기존 앵커와의 delta는 가치가 있을 수 있습니다.
2. delta를 그대로 쓰거나 강하게 반영하면 위험합니다.
3. `q98 clip + 낮은 alpha`처럼 제한된 형태로만 반영할 때 public 개선이 나왔습니다.
4. OOF 1등 후보보다 public-safe distribution band가 더 중요합니다.
5. 현재는 새 correction layer가 아니라, `a117_09`가 만든 안전한 clipped delta band를 정밀하게 찾는 단계입니다.

## 다음 작업

1. 내일 `submission_a118_01.csv` public 점수를 먼저 확인합니다.
2. 개선되면 asymmetric clipping 범위를 더 좁게 탐색합니다.
3. 개선되지 않으면 `a117_09`를 안전 앵커로 유지하고, delta 평균/최댓값 guard를 더 강하게 둡니다.
4. 10.00대 진입 목표를 위해 큰 구조 변경보다 public에서 검증된 작은 delta 구간을 더 촘촘히 찾습니다.

## 한 줄 요약

오늘은 `a117_09 = 10.1005923422`로 새 최고 기록을 만들었고, “약한 direct 모델의 전체 예측”이 아니라 “앵커와의 clipped delta”가 현재 가장 유망한 개선 축이라는 점을 확인했습니다.
