# 일일 리포트

- 생성 날짜: `2026-04-28`
- 현재 최고 public 점수: `10.1005923422`
- 현재 최고 파일: `submission_a117_09.csv`

## 오늘의 핵심 변화

- 전날 만든 `a118_01`을 제출했고 public `10.1018318718`을 확인했습니다.
- `a118_01`은 OOF와 분포 안정성 기준으로는 좋아 보였지만, public에서는 `a117_09`를 넘지 못했습니다.
- 이 결과로 “최댓값을 낮추고 비대칭 클리핑을 쓰면 더 안전할 것”이라는 가설이 깨졌습니다.
- 이후 `a119`에서는 `a117_09`와 거의 같은 형태를 유지하는 초근접 microgrid를 만들었습니다.
- 마지막 제출 후보로 `a119_03`을 선택했고 public `10.1010915332`를 확인했습니다.
- `a119_03`은 `a117_09` 대비 test 변화량이 매우 작았지만, 그래도 최고 기록을 넘지 못했습니다.

## 오늘 얻은 해석

1. `a117_09`가 현재까지 가장 강한 public 앵커입니다.
2. ridge seq2seq delta 계열은 실제로 한 번 유효했지만, `a117_09` 근처에서 거의 포화된 것으로 보입니다.
3. `a118`처럼 분포 shape를 조금 바꾸면 public이 나빠졌습니다.
4. `a119`처럼 거의 안 바꿔도 public 개선이 나오지 않았습니다.
5. 따라서 같은 clipped delta family 안에서 alpha, q, tail clip을 더 깎는 방식은 기대값이 낮습니다.
6. 9점대 진입을 위해서는 `a117_09`를 앵커로 두되, ridge delta와 다른 독립 신호를 찾아야 합니다.

## 오늘 제출 결과

| 실험 | 제출 파일 | public | 해석 |
| --- | --- | ---: | --- |
| a118 | `submission_a118_01.csv` | `10.1018318718` | 비대칭 clipped delta. OOF는 좋았지만 public 악화 |
| a119 | `submission_a119_03.csv` | `10.1010915332` | `a117_09` 초근접 microgrid. 안전했지만 개선 실패 |

## 현재 최고 기록

- 최고 public: `submission_a117_09.csv = 10.1005923422`
- 이전 주요 기준: `submission_a114_09.csv = 10.103316418`
- 오늘 제출한 `a118`, `a119`는 모두 최고 기록을 넘지 못했습니다.

## 다음 방향

현재 기준으로 가장 합리적인 다음 방향은 아래와 같습니다.

1. `a117_09`를 최종 안전 앵커로 유지
2. ridge seq2seq delta family의 추가 미세조정은 우선순위 하향
3. `a114`의 future-window scenario baseline/scale 해석은 유지
4. 9점대 진입을 위해서는 새로운 독립 신호가 필요
5. 후보 방향은 residual error 상위 그룹을 다시 EDA하고, `a117_09`가 틀리는 시나리오의 공통 운영 패턴을 찾는 것
6. 다음 실험은 “delta를 얼마나 섞을까”가 아니라 “어떤 시나리오에서 현재 앵커가 구조적으로 틀리는가”를 다시 파고드는 쪽이 맞음

## 한 줄 요약

오늘은 `a118`, `a119` 제출을 통해 clipped ridge delta 계열의 한계를 확인했습니다. `a117_09`는 여전히 최고 기록이며, 9점대 진입을 위해서는 같은 family의 미세조정보다 새로운 독립 신호와 오류 그룹 분석이 필요합니다.
