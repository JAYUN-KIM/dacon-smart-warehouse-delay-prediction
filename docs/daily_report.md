# 일일 리포트

- 생성 날짜: `2026-04-29`
- 현재 최고 public 점수: `10.0960054075`
- 현재 최고 제출 파일: `submission_a124_1443.csv`
- 다음 작업일: `2026-04-30`

## 오늘의 핵심 변화

오늘은 `a122_1045`에서 확인된 late/high-stress uplift 축을 이어서 검증했습니다.

가장 중요한 흐름은 다음과 같습니다.

- `a123_059`는 `a122_1045`와 거의 같은 점수인 `10.0967996581`을 기록했습니다.
- 이 결과로 stress tail에 조금 더 얹는 two-tier 방식은 추가 개선을 만들지 못한다고 판단했습니다.
- 이후 `a124`에서는 보정 총량을 크게 늘리는 대신, `a122_1045`의 flat top-stress uplift를 late slot 쪽으로 옮기는 mean-preserving late shift를 탐색했습니다.
- `submission_a124_1443.csv`가 public `10.0960054075`를 기록하며 최고 기록을 갱신했습니다.

## 오늘 확인한 실험

| 실험 | 결과 | 해석 |
| --- | ---: | --- |
| `a123_059` | `10.0967996581` | `a122_1045`와 사실상 동점. stress tail 추가만으로는 개선 제한 |
| `a124_1443` | `10.0960054075` | late-weighted stress shift가 public에서 추가 개선 |

## 현재 판단

1. 최고 기록은 `a124_1443 = 10.0960054075`입니다.
2. `a122_1045`의 late/high-stress 보정 축은 여전히 유효합니다.
3. 단순히 stress tail에 더 얹는 방식은 거의 개선이 없었습니다.
4. public은 “얼마나 더 올리느냐”보다 “어느 slot 쪽으로 보정을 옮기느냐”에 더 반응하고 있습니다.
5. 따라서 다음 실험은 `a124_1443` 주변에서 late shift 파라미터를 더 촘촘히 탐색하는 것이 맞습니다.

## 내일 방향: a125

내일은 `2026-04-30` 기준으로 `a125`부터 이어갑니다.

권장 방향:

- `a124_1443`을 새 public 앵커로 유지
- `q=0.900`, `target_mean=0.055`, `floor=0.45`, `start=12`, `denom=8`, `power=0.75` 주변을 정밀 탐색
- 평균 uplift는 `0.052~0.058` 근처 유지
- max uplift는 `0.80~0.90` 근처 유지
- coverage는 `0.095~0.105` 근처 유지
- `a124_1443`보다 과하게 max uplift를 올리는 후보는 별도 공격형 후보로 분리

## 한 줄 요약

오늘은 `a124_1443 = 10.0960054075`로 최고 기록을 다시 갱신했고, 다음 주력은 `a125`에서 late-weighted stress shift를 더 정밀하게 다듬는 것입니다.
